//
//  HelperUtility.swift
//  NeoFitness
//
//  Created by Sai Ram on 23/11/19.
//  Copyright © 2019 dmondo. All rights reserved.
//

/*
import UIKit

    @objc class SaveToDefaults: NSObject {
    
    func saveUser(bool: Bool) {
        
        let userDefaults = UserDefaults.standard
        userDefaults.set(bool, forKey: Defaults.currentUser)
        userDefaults.synchronize()
    }
    
    func getUser() -> Bool {
       
        let userDefaults = UserDefaults.standard
        let bool = userDefaults.bool(forKey: Defaults.currentUser)
        return bool
        
    }
        
        static func setBranchId(branchId: String) {
               print(branchId)
               let defults = UserDefaults.standard
            defults.set(branchId, forKey: Defaults.branchId)
               defults.synchronize()
    
           }
    
          static func getbranchId() -> String {
                let defults = UserDefaults.standard
            let value: String = defults.string(forKey: Defaults.branchId) ?? ""
    
                print(value)
               return value
    
    
           }
}
 */
